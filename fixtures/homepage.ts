// @todo turn this into dynamic generator 

import { Bound } from "types/bounds";

export const get_all_bounds_fixture = ():Bound[] => {
  return [
      {
        name: "Personal",
        slug: "personal"
      }
  ];
}